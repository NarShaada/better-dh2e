# Secrets — never commit
creds.txt

# Visual-companion scratch (design mockups, regenerated per session — not shipped)
mockups/
.superpowers/

# Large reference clones / extracted systems (study material, not source)
reference/

# OS noise
.DS_Store

# Node
node_modules/
npm-debug.log*

# Build artifact (uploaded as a release asset, not committed)
better-dh2e.zip

# Dev plans/specs — not part of the shipped system
docs/
