{
  "name": "better-dh2e",
  "version": "0.1.0",
  "description": "A fresh, Foundry-v13-native Dark Heresy 2e game system.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "bash tools/deploy.sh"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
